---
name: 自动
description: "情圣子命令：生成可审核的聊天草稿及条件分支，可建议发送、等待、观察或停止。"
---

先读情圣 `SKILL.md`、`references/user-context.md` 和 `references/autopilot-guide.md`，使用同一个 `GOUTOU_DATA_DIR`。

第一行按当前行动使用 `[发送]`、`[等她回复]`、`[观察]` 或 `[停止]`。只有适合回复时才给消息；不给尚未回复后的自动追发安排。后续分支依赖对方真实回应，不代替用户发送，不把草稿记作已执行。

输入 `/顾问` 切回普通分析模式。
