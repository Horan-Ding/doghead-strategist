# goutou-data（私有）

本仓库只存狗头军师 **私有 data**，与 `doghead-strategist` 代码仓分离。

目录即 `GOUTOU_DATA_DIR` 根：

```text
config.yaml
me/profile.yaml
people/<codename>/
  meta.yaml
  state.yaml
  conversations/normalized.jsonl
```

同步说明见代码仓：`doghead-strategist/docs/private-data-and-sync.md`。
